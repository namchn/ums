
```
간략 흐름도

┌────────────────────────────┐
│ 1. REST API 발송 요청       │
│ POST /api/messages         │
└─────────────┬──────────────┘
              │
              ▼
┌────────────────────────────┐
│ Message 저장 (원본 데이터)   │
└─────────────┬──────────────┘
              │
              ▼
┌────────────────────────────┐
│ Outbox 저장 (발송 큐)       │
└─────────────┬──────────────┘
              │
             (비동기)
              │
              ▼
┌────────────────────────────┐
│ OutboxDispatcher           │
│ (5초마다 미발행 Outbox 조회) │
├────────────────────────────┤
│ SMTP/메일 API 호출(시뮬)     │
│ published = true 변경       │
└────────────────────────────┘

```